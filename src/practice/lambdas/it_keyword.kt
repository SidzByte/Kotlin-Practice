package practice.lambdas

//If a lambda has only one parameter, Kotlin provides it:

//val square: (Int) -> Int = { it * it }
//println(square(4))  // Output: 16

// it * it is shorthand for { x -> x * x }.